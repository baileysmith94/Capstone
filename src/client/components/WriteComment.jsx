import React, { useState, useEffect } from "react";
import { updateReview } from "../../server/api";

function CommentForm(token) {
  const writeComment = async () => {
    const response = await fetch(`http://localhost:3000/api/`);
  };
}
